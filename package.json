{
	"name": "giggle-tribes-jade",
	"version": "1.0.0",
	"main": "index.js",
	"engines": {
		"node": "12.x",
		"yarn": "1.x"
	},
	"scripts": {
		"start": "cd ~/backend && yarn start",
		"build": "cd backend && yarn install && yarn db up && cd ../frontend && yarn install && yarn build && mv build/* ../backend/src/static"
	},
	"author": "",
	"license": "ISC"
}
